# Browser support configuration
# Supports all modern browsers (Chrome 60+, Firefox 60+, Safari 12+, Edge 79+)
# Note: IE11 is NOT supported due to React 19 requirements

# Modern browsers (last 2 versions)
last 2 versions

# Specific browser versions
Chrome >= 60
Firefox >= 60
Safari >= 12
Edge >= 79
iOS >= 12
Android >= 6

# Not dead browsers (browsers with >0.5% usage)
not dead

# Explicitly exclude IE11 (React 19 doesn't support it)
not IE 11

